server:
  applicationConnectors:
    - type: http
      port: ${PORT:-50410}

logging:
  level: INFO
  loggers:
    "uk.gov": DEBUG
  appenders:
    - type: console
    - type: file
      currentLogFilename: logs/stub-verify-hub.log
      archivedLogFilenamePattern: logs/stub-verify-hub.log.%d.gz

helloWorldValue: ${HELLO_WORLD_VALUE:-World}
