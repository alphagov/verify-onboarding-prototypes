server:
  type: simple
  applicationContextPath: /
  adminContextPath: /admin
  connector:
    type: http
    port: ${PORT:-50410}

logging:
  level: INFO
  loggers:
    "uk.gov": DEBUG
  appenders:
    - type: console
